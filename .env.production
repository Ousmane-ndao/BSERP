# Prod Vercel (bserp.vercel.app) : pas de VITE_API_URL ici — le code utilise /api + vercel.json (proxy → Render).
# Ne pas mettre l’URL Render directe : le navigateur bloque en CORS.
VITE_FRONTEND_URL=https://bserp.vercel.app
